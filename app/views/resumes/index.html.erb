<div class="page-header">
  <h1>
    Resumes
    <a href="/resumes/new" class="btn btn-lg btn-success">New Resume</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>User</th>
        <th>Name</th>
        <th>Actions</th>
      </tr>

      <% @resumes.each do |resume| %>
      <tr>
        <td>
          <% if resume.user.present? %>
            <a href="/users/<%= resume.user.id %>">
              <%= resume.user.username %>
            </a>
          <% end %>
        </td>
        <td><%= resume.name %></td>
        <td>
          <a href="/resumes/<%= resume.id %>" class="btn btn-primary">Show</a>
          
          <% if current_user == resume.user %>
            <a href="/resumes/<%= resume.id %>/edit" class="btn btn-warning">Edit</a>
            <a href="/delete_resume/<%= resume.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
          <% end %>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @resumes, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
